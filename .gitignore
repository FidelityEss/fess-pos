# Dependencies and builds
node_modules/
.pnpm-store/
dist/
build/
.next/
out/
coverage/
*.tsbuildinfo

# Dart / Flutter
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
# Library lockfiles aren't committed; the example app's is (DEVELOPMENT-GUIDELINES §5)
packages/fess_pos/pubspec.lock
packages/fess_pos_engine/pubspec.lock

# Secrets and local environment — never commit
.env
.env.*
!.env.example
**/.env.local
supabase/functions/.env*
!supabase/functions/.env.example
supabase/.temp/
supabase/.branches/

# OS / editor
.DS_Store
.idea/
.vscode/
