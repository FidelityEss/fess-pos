{
  "$comment": "DEMO. The inspection journey (docs/04 §3.2, step catalogue docs/11 §6). Steps run in order unless a step's `next` rule branches. location_check, declaration and submit are integrity steps: they can be moved and parameterised, never removed.",
  "spec_version": "1.0",
  "kind": "flow",
  "family": "site_inspection_flow",
  "scope": { "bank_id": null },
  "version": 1,
  "form_family": "site_inspection",
  "action": "inspection.submit",
  "outcomes": "inspection",
  "steps": [
    { "id": "briefing", "type": "job_briefing", "view": "job_detail",
      "props": { "acknowledge_text": "I have read the job details and I'm at the merchant's premises." } },

    { "id": "location", "type": "location_check",
      "$comment": "Uses the job's location profile (docs/07 §7). Relaxed profiles prompt a check-in on arrival; with no GPS lock inside, a fix taken immediately outside the premises counts (PM, 2026-09-11)." },

    { "id": "arrival", "type": "form", "sections": ["arrival"],
      "next": { "if": [
        { "==": [ { "var": "answers.business_open" }, false ] },
        "flow:unable_to_complete_flow",
        "profile" ] } },

    { "id": "profile",   "type": "form", "sections": ["merchant_profile", "payments"], "paging": "section_per_page" },
    { "id": "premises",  "type": "form", "sections": ["premises"], "paging": "single_page" },
    { "id": "activity",  "type": "form", "sections": ["risk_indicators"], "paging": "single_page" },
    { "id": "interview", "type": "form", "sections": ["interviewee"] },

    { "id": "review", "type": "summary_review", "props": { "show_risk_indicators": true, "allow_jump_back": true } },

    { "id": "declare", "type": "declaration", "sections": ["agent_declaration"] },
    { "id": "submit",  "type": "submit" }
  ]
}
